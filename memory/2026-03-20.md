# 2026-03-20

## Trading Bugs Fixed

### Bug 1: SignedOrder stale code (~00:45 UTC)
- `/polymarket/src/polymarket_mcp/auth/client.py` had old version where `post_order()` returned `SignedOrder` directly without submitting
- Fix: synced from repo — now does `create_order()` → `SignedOrder` → `post_order()` → dict response
- Affected: Iowa/Clemson, Santa Clara/Kentucky, Missouri/Miami, Bucks/Jazz, 76ers/Kings, Utah/Golden Knights — all failed, no trades executed

### Bug 2: 401 Unauthorized (~00:52 UTC)
- After SignedOrder fix, trades now fail with 401 "Unauthorized/Invalid api key"
- L2 API key invalid or expired
- **Action needed**: regenerate API key via `derive_api_key()` or update creds in config
- Philipp asked about this via Telegram (agent message sent)

### Cron Jobs: All 12 Registered
- Jobs 1-3: registered by previous subagent session (aborted)
- Jobs 4-12: registered in this session
- Missouri/Miami (0x4491) still returning 404 on CLOB price endpoint — market may lack CLOB liquidity, retrying at 01:30

### Duplicate cron job
- Two entries for `watch:0x4491df698adf8d` at 01:00Z (IDs: 26372bed and 365736e4) — one has no announce target, one does. May want to clean up.
